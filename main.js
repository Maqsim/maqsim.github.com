function FMCI(numbers) {
  let freq = {};

  numbers.forEach(number => {
    if (!freq[number]) {
      freq[number] = 0;
    }
  });

  numbers.forEach(number => {
    numbers.forEach(number => {

    });
  });
}
